id: aileron-002
title: Alert on secret exfiltration patterns in tool arguments
severity: critical
match:
  type: tool_call
  tool.arguments_contains:
    - "id_rsa"
    - ".env"
    - "aws_secret"
    - "BEGIN PRIVATE KEY"
action: alert
